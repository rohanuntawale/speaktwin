[pytest]
testpaths = tests
# `tests` on the path lets test modules import the audio builders from
# conftest directly; the repo root lets them import `backend.*`.
pythonpath = . tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
